// const mark = {
//   fullName: "Mark Miller",
//   mass: 78,
//   height: 1.69,
//   CalcBMI: function () {
//     this.BMI = this.mass / (this.height * this.height);
//     return this.BMI;
//   },
// };
// const john = {
//   fullName: "John Smith",
//   mass: 92,
//   height: 1.95,
//   CalcBMI: function () {
//     this.BMI = this.mass / (this.height * this.height);
//     return this.BMI;
//   },
// };
// // calculate the BMI one time and use the result in multiple situations
// const markBMI = mark.CalcBMI();
// const johnBMI = john.CalcBMI();
// if (mark.BMI > john.BMI) {
//   console.log(
//     `${mark.fullName}'s BMI (${mark.BMI}) is heigher than ${john.fullName}'s BMI (${john.BMI}) `
//   );
// } else if (john.BMI > mark.BMI) {
//   console.log(
//     `${john.fullName}'s BMI (${john.BMI}) is heigher than ${mark.fullName}'s BMI (${mark.BMI}) `
//   );
// }
